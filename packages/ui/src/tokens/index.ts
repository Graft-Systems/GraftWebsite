// @graft/ui/tokens
//
// Brand design tokens shared across web and mobile.
//
// At M0-01 this is empty; the actual tokens (colors burgundy/amber/sage,
// typography scale, spacing scale, font CSS variables) are extracted from
// apps/web/tailwind.config.ts here in M0-02 alongside the Spray app shell
// work, so that apps/web and apps/mobile can both consume them without
// duplication.

export const placeholder = "tokens-pending-m0-02" as const;
